#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main()
{
    // cout << "  *" << endl
    //      << " ***" << endl
    //      << "*****" << endl;

    // int a; // interger 1 23 4 5 -1 -2
    // int b; // interger
    // int c; // inter
    // int d; // interger
    // int a;
    // float b; // 1,2 2,3 4,56

    // // khong duoc ky tu dac biet !@#$%^&*
    // // khong duoc bat dau bang so
    // int so_luong_sinh_vien;
    // int soLuongSinhVien;

    // int a = 100000000000;
    // long a = 100000000000;
    // string a;

    // cin >> a;

    // getline(cin, a);

    // int a = 2, b = 3;

    // cout << a + b << endl;

    // string aa = "tuan", bb = "anh";

    // cout << aa + bb << endl;

    // cin >> a >> b >> c;

    // cout << a << " " << b << " " << c << endl;

    // int a = 1;

    // a--;
    // cout << a;

    // int a = 4;

    // cout << sqrt(a) << endl;

    // bool a;
    // a = 0;
    // cout << boolalpha << a << endl;

    // int a = 10, b = 3;
    // cout << a + b << endl;
    // cout << a - b << endl;
    // cout << a * b << endl;
    // cout << a / b << endl;
    // cout << a % b << endl;

    // int a = 8;
    // bool is_so_chan;

    // is_so_chan = (a % 2 == 0);

    // cout << boolalpha << is_so_chan << endl;

    // bool a = (3 > 4);

    // cout << boolalpha << a << endl;

    // int a, b, c;

    // cin >> a >> b >> c;

    // bool ba_so_bang_nhau = (a == b && b == c);

    // cout << boolalpha << ba_so_bang_nhau << endl;

    // int a = 1, b = 2, c = 3, d = 4, e = 5, f = 6;
    // float tong_diem = ((a + b + c) + (d + e) * 2 + f * 3);

    // cout << fixed << setprecision(2) << 4.99999999999 << endl;

    string a = "tuaádkhjn", b = "anh";
    cout << "Hello " << a << " and " << b << "." << endl;

    return 0;
}
