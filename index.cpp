#include <iostream>
#include <string.h>
#include <cmath>
#include <iomanip>

using namespace std;

int main()
{

    //   *
    //  ***
    // *****

    // cout << "  *" << endl;
    // cout << " ***" << endl;
    // cout << "*****" << endl;

    // int a = 1000000000; //interger

    // cout << a << endl;
    // float b; // 1.2 1.3 1.12
    // long c;

    // // int16:

    // int b = 1;

    // cout << a + b << endl;

    // int a;
    // int b;
    // cin >> a;
    // cin >> b;

    // cout << a << " " << b << endl;
    // cout << "tong 2 so " << a + b << endl;

    // string hotensinhvien;

    // // cin >> hotensinhvien;
    // getline(cin, hotensinhvien);

    // cout << hotensinhvien << endl;

    // intput: 21/07/2021

    // output
    // ngay 21
    // thang 07
    // nam 2021

    // int a, b, c;
    // string ngay, thang, nam;

    // cin >> ngay  "/" >> thang >> "/" >> nam >> endl;

    // int a, b, c;

    // // int hoc_sinh;
    // // int hocSinh;

    // cin >> a;

    // // cout << a + b << endl;
    // // cout << a - b << endl;
    // // cout << a * b << endl;
    // // cout << a / b << endl;
    // // cout << a % b << endl; // phan du

    // cout << sqrt(a) << endl;

    // int a = 4;

    // b = false;

    // bool b = true;
    // bool a = false;

    // cout << b << endl;

    // int a = 4;
    // int b = 3;

    // bool c = (a < b); // false
    // bool d = false;

    // cout << boolalpha << (c == d) << endl;

    // int a;
    // cin >> a;

    // bool is_so_chan;

    // is_so_chan = (a % 2 == 0);

    // cout << boolalpha << is_so_chan << endl;
    // a % 2 == 0
    // a % 2 == 1

    // int a, b, c;

    // cin >> a >> b >> c;

    // cout << boolalpha << (a == b && a == c) << endl;

    // int a = 1, b = 2, c = 3, d = 4, e = 5, f = 6;

    // // cin >> a >> b >> c >> d >> e >> f;

    // int dtb = (a + b + c) + (d + e) * 2 + f * 3;
    // float he_so = 10;

    // cout << fixed << setprecision(5) << "dtb = " << 4.99999999999 << endl;

    string a = "tuan";
    string b = "anh";

    cout << "Hello " << a << " and " << b << "." << endl;

    return 0;
}

// AND && operator
// true true => true
// true false => false
// false false => false

// OR ||
// true true => true
// true false => true
// false false => false